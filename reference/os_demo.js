const os = require('os');

// Platform
console.log(os.platform());

// Cpu Arc
console.log(os.arch());

// Cpu Core Info
console.log(os.cpus());

// Free Memory
console.log(os.totalmem());

// Total Memo
console.log(os.freemem());

// System up Time
console.log(os.uptime());
